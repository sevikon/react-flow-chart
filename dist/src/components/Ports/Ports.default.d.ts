import * as React from 'react';
export interface IPortsDefaultProps {
    children: Array<React.ReactElement<any>>;
}
export declare const PortsDefault: ({ children }: IPortsDefaultProps) => JSX.Element;
